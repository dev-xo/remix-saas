import { describe, expect, it } from 'vitest'

describe('Example of an unit test.', () => {
	it('Should successfully pass.', () => {
		const hasVitestConfiguredSuccessfully = true
		expect(hasVitestConfiguredSuccessfully).toBeTruthy()
	})
})
